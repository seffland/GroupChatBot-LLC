services:
  bot:
    build: .
    env_file:
      - .env
    environment:
      - OLLAMA_URL=http://host.docker.internal:11435
    networks:
      - plexllm_app-network

networks:
  plexllm_app-network:
    external: true
